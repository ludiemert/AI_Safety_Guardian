About project

VISÃO PROFISSIONAL (ARQUITETURA)
🔷 BACKEND (Flask)
👉 Responsável por:
processar dados
ler CSV
criar API

🔷 API (ponte entre back e front)
👉 Responsável por:
enviar dados do backend para o frontend

🔷 FRONTEND (HTML + JS)
👉 Responsável por:
pedir dados
mostrar dados
criar gráfico

VISÃO COMPLETA (EMPRESA)
CSV (storage)
↓
Flask (backend)
↓
API (JSON)
↓
JavaScript (frontend)
↓
Plotly (visualization)
↓
User (dashboard)


✔ Backend API
✔ Data pipeline
✔ Frontend fetch
✔ Data visualization
The backend saves data.
The API sends data.
The frontend gets data.
Plotly shows the chart.

I created a Flask API to read data from CSV.
The frontend uses fetch to get the data.
I used Plotly.js to create charts.


Start Project

Always
changed Python → restart server
changed HTML/CSS/JS → refresh browser


Start Backend

(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian> python backend/app.py

Start Force Backend
.\.venv\Scripts\python.exe backend\app.py



______________________

Trilha do projeto

Fase 1: imagem upload + alarme + lógica simples
Fase 2: histórico + gráfico
Fase 3: vídeo/webcam contínuo
Fase 4: snapshot automático do risco
Fase 5: YOLO detectando riscos reais
Fase 6: relatório PDF/dashboard
Fase 7: deploy/demo online







___________

Partes do Project

Now:
Agora temos esta base:

✔ Flask
✔ HTML/CSS/JS
✔ Upload de imagem
✔ OpenCV processando
✔ Resultado na tela
✔ Nomes únicos
✔ Projeto organizado

Criar o “Risk Detection Logic”

Antes de YOLO, vamos criar uma lógica simples de risco:

imagem enviada → sistema processa → cria um status:
Safe
ou
Risk detected

Exemplo inicial:

se a imagem tiver muitas bordas/objetos → Possible risk detected
se tiver poucas bordas → Safe

Ainda não é YOLO, mas já prepara o projeto para: YOLO → detect person → detect helmet → risk alert

O que vamos adicionar

Na página de resultado:

Safety Status: Possible Risk Detected
Risk Level: Medium
Message: Please check the area.

Depois:

áudio automático quando risco for detectado
salvar histórico
gráfico
YOLO

JA TEMOS
✔ upload de imagem
✔ OpenCV processa imagem
✔ Edge Score
✔ risco acima de 20%
✔ alarme automático contínuo
✔ botão Stop Alarm
✔ botão Start Alarm
✔ resultado na tela

OQ ja esta concluido: 
OpenCV → processa a imagem
Risk logic → decide Safe ou Risk
CSV → salva o histórico
Alarm → responde com alerta sonoro


Proximo passo:
Salvar histórico + criar gráfico

Esse é o melhor próximo passo porque transforma o projeto em sistema real, não só teste de imagem.

Vamos salvar:

date/time
edge score
risk level
status
image path


Ok funcionando e criando tabela com as informacoes da img no historico

AGORA

Criar a página /history

Para você ver o histórico na web, não só no CSV.


Pagina History
Como será o history.html
👉 vai mostrar uma tabela com dados

🧩 history.html


AGORA ESTA PRONTO

✔ upload de imagem
✔ processamento (OpenCV)
✔ decisão (risk logic)
✔ alarme
✔ histórico (CSV)
✔ visualização web (/history)
✔ UI organizada


Depois disso vamos para:

CSV → tabela na web → gráfico → relatório

mais grafico

Fluxo completo
Flask → envia JSON
↓
JS → recebe JSON
↓
Plotly → transforma em gráfico
↓
Browser → mostra visual
💥 Tradução prática
Python pensa
JavaScript pede
Plotly desenha
Usuário entende 


Frase A2 (IMPORTANTE)
The backend calculates the average.
The frontend receives the data.
Plotly shows the chart.
🚀 O QUE VOCÊ TEM AGORA
✔ coleta dados
✔ calcula métricas
✔ cria indicadores (KPI)
✔ mostra visualmente

👉 Isso já é:  mini data analytics system

vou trocar o app.py para 2_app.py (aqui a versao com graficos)
e tambem voou deixar a parte do history guardada

_____________________________________
Outro nivel

projeto: YOLO (detecção real)
YOLO = You Only Look Once

modelo de IA que detecta objetos na imagem


PRÓXIMO NÍVEL (AGORA FICA MUITO FORTE)

Agora você pode evoluir para:

🔥 YOLO (detecção real de risco)

👉 pessoa sem capacete
👉 objeto no chão
👉 área bloqueada

now AI Object Detection System
install yolo


TESTE NO POWERSHELL para ver o .env ativo

[notice] A new release of pip is available: 26.1 -> 26.1.1
[notice] To update, run: python.exe -m pip install --upgrade pip
(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian> where python
(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian> python -c "import sys; print(sys.executable)"
C:\Users\user\Downloads\AI_Safety_Guardian\.venv\Scripts\python.exe
(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian> .\.venv\Scripts\activate
(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian> python -c "from ultralytics import YOLO; print('YOLO OK')"
Creating new Ultralytics Settings v0.0.6 file
View Ultralytics Settings with 'yolo settings' or at 'C:\Users\user\AppData\Roaming\Ultralytics\settings.json'
Update Settings with 'yolo settings key=value', i.e. 'yolo settings runs_dir=path/to/dir'. For help see https://docs.ultralytics.com/quickstart/#ultralytics-settings.
YOLO OK
(.venv) PS C:\Users\user\Downloads\AI_Safety_Guardian>


Como será a lógica correta no futuro?

pessoa + sem capacete = risco
pessoa + caixa muito grande = risco médio
caixa no chão bloqueando caminho = risco
caixa na prateleira = seguro
pessoa normal caminhando = seguro

ate agora o projeto tem:
✔ Flask backend
✔ Upload de imagem
✔ OpenCV
✔ YOLO object detection
✔ Alarm logic
✔ CSV history
✔ API
✔ Plotly dashboard
✔ Web interface